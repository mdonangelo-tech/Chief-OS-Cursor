# Database
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
# Create DB first: createdb chief_os (or via psql)
DATABASE_URL="postgresql://localhost:5432/chief_os?schema=public"

# Auth.js
AUTH_SECRET="run: npx auth secret"
AUTH_URL="http://localhost:3000"

# Public API base URL (used by frontend when calling API subdomain)
# In prod, set to: https://api.chief-os.ai
NEXT_PUBLIC_API_BASE_URL="http://localhost:3000"

# Email (magic links)
# Provider: console (dev, logs links) | resend | sendgrid
EMAIL_PROVIDER=console

# Optional: Real email provider (when not using console)
# AUTH_EMAIL_FROM="noreply@yourdomain.com"
# RESEND_API_KEY=""
# SENDGRID_API_KEY=""

# Google OAuth (sign-in + Gmail/Calendar connect)
# Add both redirect URIs in Google Cloud Console:
#   - {AUTH_URL}/api/auth/callback/google     (sign-in)
#   - {AUTH_URL}/api/connect-google/callback  (connect account)
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# Dogfood allowlist (comma-separated). If set, only these emails can sign in.
DOGFOOD_ALLOWED_EMAILS=""

# Token encryption (required for Google refresh tokens)
# Run: openssl rand -hex 32
ENCRYPTION_KEY=""
# Back-compat (supported): TOKEN_ENCRYPTION_KEY=""

# LLM (email classification + calendar analysis)
# Recommend: OpenAI gpt-4o-mini (fast, cheap, good JSON). Anthropic Claude Haiku works too.
# Auto-enables when API key is set. Set LLM_CLASSIFICATION_ENABLED=false to disable.
# LLM_CLASSIFICATION_ENABLED=true
# LLM_PROVIDER=openai
# OPENAI_API_KEY="sk-..."
# OPENAI_MODEL=gpt-4o-mini
# ANTHROPIC_API_KEY=""
# ANTHROPIC_MODEL=claude-3-haiku-20240307
# Cost controls (per sync): batch = 1 API call for N emails
# LLM_BATCH_SIZE=6     # emails per batch (6 emails = 1 call)
# LLM_MAX_BATCHES=2    # max batches per sync (12 emails, 2 calls)
# LLM_CALENDAR_PER_SYNC=5   # calendar events to enrich per sync

# Gmail rate limit (requests per minute per account)
# GMAIL_RATE_LIMIT_RPM=60

# Private mode (edge Basic Auth gate for ALL routes)
# When PRIVATE_MODE=true, BASIC_AUTH_USER and BASIC_AUTH_PASSWORD are required
PRIVATE_MODE="false"
BASIC_AUTH_USER=""
BASIC_AUTH_PASSWORD=""
